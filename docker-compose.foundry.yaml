services:
  foundry:
    image: felddy/foundryvtt:latest
    volumes:
      - type: bind
        source: .data
        target: /data
    environment:
      - FOUNDRY_HOT_RELOAD=true
      - FOUNDRY_TELEMETRY=false
    env_file:
      - .env
    ports:
      - 30000:30000
