{
  "name": "@rpgmadesimple/monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prepare": "turbo run prepare --continue",
    "build": "turbo run build --continue",
    "dev": "turbo run dev --continue",
    "lint": "turbo run lint --continue",
    "fix": "turbo run fix --continue",
    "test:unit": "turbo run test:unit --continue",
    "coverage": "turbo run coverage --continue"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "turbo": "^2.7.1",
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=21"
  }
}
