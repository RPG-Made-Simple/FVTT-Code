export class AbsolutePhantasiaActor extends Actor {
  override prepareData(): void {
    super.prepareData();
  }

  override prepareDerivedData(): void {
    const system = this.system;

    if (this.type === "character") {

    }
  }
}
