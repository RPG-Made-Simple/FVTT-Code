{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "prepare": {
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    },
    "build": {
      "dependsOn": ["^build", "prepare"],
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["build"],
      "inputs": ["$TURBO_DEFAULT$", ".env"]
    },
    "lint": { },
    "test": { },
    "fix": {
      "cache": false
    }
  }
}
